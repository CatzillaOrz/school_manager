.content-container.gray-bg
    .new-notice-container
        ul.new-notice-content
            li.new-notice-list(ng-repeat="notice in noticeListFn.noticeList" ui-sref="noticedetail({id:notice.id})")
                .new-notice-image
                    img(ng-src="{{indexFn.getHtmlFirstImg(notice.content) || 'https://s.aizhixin.com/notice-default420x156.png'}}")
                .new-notice-content
                    .date
                        span.day {{ noticeListFn.formatDate(notice.publishDate,'dd') }}
                        span.year /{{ noticeListFn.formatDate(notice.publishDate,'yyyy-MM') }}
                    .name
                        | {{noticeListFn.cutHtml(notice.title,36)}}
                    .introduce
                        | {{noticeListFn.cutHtml(notice.content,100)}}
    .page.gray-bg(ng-if='noticeListFn.page.totalElements')
        ul.pagination-sm(uib-pagination, total-items='noticeListFn.page.totalElements',items-per-page="noticeListFn.page.pageSize" max-size='noticeListFn.page.pageSize' ng-model='noticeListFn.page.pageNumber',ng-change="noticeListFn.getNoticeList()" boundary-link-numbers='true' rotate='false' previous-text="<" next-text=">")

