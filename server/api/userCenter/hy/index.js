'use strict';

var express = require('express');
var controller = require('./hy.controller');
var auth = require('../../../middleware/auth');

var router = express.Router();

// router.get('/getXxx',  controller.getXxx);

module.exports = router;
